services:
  mysql:
    image: 'mysql:8.0'
    restart: unless-stopped
    environment:
      - 'MYSQL_DATABASE=nexus_banking_db'
      - 'MYSQL_ROOT_PASSWORD=root'
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
